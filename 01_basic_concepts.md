# 챕터 01: 기초 개념

> Nemoclaw를 이해하기 위해 꼭 알아야 할 용어들을 쉽게 설명합니다.

---

## 1. 프로세스(Process)란?

컴퓨터에서 실행 중인 프로그램 하나하나를 **프로세스**라고 합니다.

예를 들어, 크롬 브라우저를 켜면 "크롬 프로세스"가 실행됩니다. 동시에 카카오톡을 켜면 "카카오톡 프로세스"도 실행됩니다. 이 두 프로세스는 서로 독립적으로 동작합니다.

```
[내 컴퓨터]
  ├── 크롬 프로세스  (인터넷 서핑 중)
  ├── 카카오톡 프로세스  (메시지 수신 대기)
  └── AI 에이전트 프로세스  (작업 처리 중)
```

---

## 2. 포트(Port)란?

### 비유: 항구의 부두

항구(서버 컴퓨터)에는 여러 개의 부두(포트)가 있습니다. 각 부두는 특정 화물선(서비스)만 들어올 수 있도록 지정되어 있습니다.

```
[내 컴퓨터 = 항구]
  포트 80   ← 일반 웹사이트 (HTTP)
  포트 443  ← 보안 웹사이트 (HTTPS)
  포트 8080 ← Nemoclaw 기본 포트
  포트 18789← OpenShell 게이트웨이 기본 포트
```

### 왜 중요한가?

- 포트를 열어둔다는 것은 "이 번호로 외부에서 접근을 허용한다"는 의미입니다.
- 불필요한 포트를 열어두면 외부 공격자가 침입할 수 있는 문이 늘어납니다.
- **모든 열린 포트는 잠재적인 공격 경로입니다.**

### localhost vs 0.0.0.0

이 두 가지는 보안에서 매우 중요한 차이가 있습니다.

| 구분 | 의미 | 보안 위험 |
|------|------|-----------|
| `127.0.0.1` (localhost) | 내 컴퓨터 내부에서만 접근 가능 | 낮음 |
| `0.0.0.0` | 같은 네트워크의 모든 기기에서 접근 가능 | **높음** |

```
[잘못된 설정 예시]
gateway.bind = "0.0.0.0:8080"  ← 위험! 외부에서도 접근 가능

[올바른 설정 예시]
gateway.bind = "127.0.0.1:8080"  ← 안전. 내 컴퓨터에서만 접근 가능
```

---

## 3. 게이트웨이(Gateway)란?

### 비유: 건물 출입구 경비원

게이트웨이는 **모든 데이터가 들어오고 나가는 관문(경비원)**입니다.

```
외부 인터넷
     |
     v
[게이트웨이] ← 여기서 검사
     |
     v
내부 서비스들
(AI 에이전트, 데이터베이스, 파일 시스템 등)
```

게이트웨이는 이런 일을 합니다:
1. "이 요청이 허가된 것인가?" 확인
2. "이 데이터가 외부로 나가도 되는가?" 판단
3. 허가되지 않은 접근 차단
4. 모든 접근 기록(로그) 남기기

### Nemoclaw에서 게이트웨이의 역할

Nemoclaw의 게이트웨이는 AI 에이전트가 할 수 있는 모든 행동을 감시합니다.
- AI가 파일을 읽으려 한다 → 게이트웨이에서 허가 여부 확인
- AI가 외부 인터넷에 접속하려 한다 → 게이트웨이에서 허가 여부 확인
- AI가 명령어를 실행하려 한다 → 게이트웨이에서 허가 여부 확인

---

## 4. 컨테이너(Container)와 Docker

### 비유: 배송용 표준 컨테이너 박스

실제 물류에서 컨테이너 박스는 내용물이 무엇이든 상관없이 규격화된 크기로 운반됩니다. 선박, 기차, 트럭 어디서든 같은 방식으로 취급됩니다.

소프트웨어의 컨테이너도 비슷합니다:

```
[컨테이너 = 격리된 실행 환경]
  ┌─────────────────────────────┐
  │  앱 코드                    │
  │  + 필요한 라이브러리         │
  │  + 설정 파일                │
  │  → 어디서나 똑같이 실행됨    │
  └─────────────────────────────┘
```

**Docker**는 이 컨테이너를 만들고 관리하는 가장 유명한 도구입니다.

### 컨테이너의 장점

| 장점 | 설명 |
|------|------|
| 격리 | 컨테이너 내부 문제가 외부에 영향을 주지 않음 |
| 이식성 | 내 컴퓨터에서 잘 되면 서버에서도 잘 됨 |
| 보안 | 내부 앱이 시스템 파일에 함부로 접근하기 어려움 |

### Nemoclaw와 Docker

Nemoclaw는 Docker 위에서 동작합니다. 더 정확히는 **k3s(경량 Kubernetes)**라는 컨테이너 관리 시스템을 Docker 위에서 클러스터 형태로 실행합니다.

```
[내 컴퓨터]
  └── Docker
        └── k3s 클러스터
              ├── Nemoclaw 에이전트 컨테이너
              ├── OpenShell 게이트웨이 컨테이너
              └── Privacy Router 컨테이너
```

---

## 5. 샌드박스(Sandbox)란?

### 비유: 어린이 모래놀이터

어린이들이 모래놀이터(sandbox)에서 놀 때는 모래가 바깥으로 나가지 않습니다. 아이가 아무리 모래를 파고 뭔가를 만들어도 놀이터 안에서만 일어납니다.

소프트웨어 샌드박스도 마찬가지입니다:

```
[샌드박스 = 제한된 실행 공간]
  ┌─────────────────────────────────┐
  │  AI 에이전트가 이 안에서만 작동  │
  │                                  │
  │  허용: /sandbox 폴더, /tmp 폴더  │
  │  금지: 시스템 파일, 개인 문서     │
  │  금지: 무단 네트워크 접속         │
  └─────────────────────────────────┘
         이 경계 밖은 접근 불가
```

### Nemoclaw의 샌드박스 (OpenShell)

Nemoclaw는 **OpenShell**이라는 컴포넌트로 샌드박스를 구현합니다.
- 운영체제 커널 수준(Landlock LSM)에서 강제됩니다
- AI 에이전트가 규칙을 우회하는 것이 불가능합니다
- 샌드박스 규칙은 시작 시 고정되고, 실행 중에는 변경이 불가능합니다

---

## 6. API와 OpenAI-compatible API

### API란?

**API(Application Programming Interface)**는 "프로그램들끼리 대화하는 규격"입니다.

예를 들어, 날씨 앱이 기상청 데이터를 가져오는 방법:
```
날씨 앱 → [API 요청] → 기상청 서버
날씨 앱 ← [API 응답: 오늘 날씨는 맑음] ← 기상청 서버
```

### OpenAI-compatible API란?

OpenAI(ChatGPT 만든 회사)가 AI와 대화하는 표준 방식을 만들었습니다. 많은 AI 서비스들이 이 방식을 따릅니다.

**OpenAI-compatible**이라는 것은 "OpenAI와 같은 방식으로 대화할 수 있다"는 뜻입니다.

```
[내 앱] → POST /v1/chat/completions
               {
                 "model": "gpt-4",
                 "messages": [{"role": "user", "content": "안녕?"}]
               }
         ← {"choices": [{"message": {"content": "안녕하세요!"}}]}
```

이 형식을 쓰면, OpenAI API 코드를 그대로 두고 서버 주소만 바꿔서 다른 AI(Claude, Gemini, 로컬 모델)를 쓸 수 있습니다.

---

## 7. 정리: 용어 한눈에 보기

```
[전체 구조 비유]

내 컴퓨터 = 건물

  포트 = 건물 출입구 번호
  게이트웨이 = 경비원 (누가 드나드는지 통제)
  Docker = 건물 내 격리된 사무실
  컨테이너 = 각 사무실 (서로 독립)
  샌드박스 = 사무실에 설치된 잠금 장치
  API = 사무실끼리 주고받는 표준 양식
```

---

다음 챕터: [02 - OpenClaw 이해하기](./02_openclaw.md)
