# 챕터 03: Nemoclaw 이해하기

> NVIDIA가 만든 Nemoclaw의 구조와 핵심 컴포넌트를 이해합니다.

---

## 1. Nemoclaw란?

**Nemoclaw**는 NVIDIA가 2026년 3월에 공개한 오픈소스 AI 에이전트 보안 스택입니다.

한 줄로 요약하면: **"OpenClaw에 기업 수준의 보안 감옥을 씌운 것"**

```
Nemoclaw = OpenClaw (AI 에이전트 기능)
         + OpenShell (보안 실행 환경)
         + Privacy Router (개인정보 보호)
         + Intent Verification (행동 검증)
```

> **현재 상태**: 2026년 3월 기준 얼리 프리뷰(알파). 프로덕션 사용 권장 안 함.

---

## 2. 왜 Nemoclaw가 필요한가?

AI 에이전트가 강력해질수록 위험도 커집니다.

```
AI 에이전트가 할 수 있는 것들:
  ✅ 이메일 보내기
  ✅ 파일 삭제하기
  ✅ 외부 API 호출하기
  ✅ 코드 실행하기
  ✅ 웹 브라우징하기

만약 AI가 악의적으로 동작하거나, 공격자에게 탈취된다면?
  ❌ 개인 파일 전송
  ❌ 악성 코드 실행
  ❌ 개인정보 유출
  ❌ 시스템 파괴
```

Nemoclaw는 이런 시나리오를 **기술적으로 불가능하게** 만드는 것이 목표입니다.

---

## 3. Nemoclaw의 3층 보안 아키텍처

```
┌─────────────────────────────────────────────┐
│  Layer 3: Intent Verification (행동 검증)    │
│  "AI가 하려는 일이 정말 허가된 행동인가?"    │
├─────────────────────────────────────────────┤
│  Layer 2: Privacy Router (개인정보 보호)     │
│  "클라우드로 보내기 전에 개인정보 제거"       │
├─────────────────────────────────────────────┤
│  Layer 1: OpenShell (샌드박스 실행 환경)     │
│  "AI가 접근할 수 있는 파일/네트워크 통제"    │
└─────────────────────────────────────────────┘
```

---

## 4. 핵심 컴포넌트 상세 설명

### Layer 1: OpenShell (오픈쉘)

OpenShell은 Nemoclaw의 **보안 실행 환경**입니다. AI 에이전트가 이 안에서만 동작합니다.

**어떻게 동작하나?**

리눅스 커널의 **Landlock LSM**이라는 기능을 사용합니다.
- LSM = Linux Security Module (운영체제 보안 모듈)
- Landlock = 프로그램이 접근할 수 있는 파일/폴더를 제한하는 기능

쉽게 말하면: 운영체제 수준에서 강제로 막기 때문에 AI가 아무리 똑똑해도 우회할 수 없습니다.

```
OpenShell이 허용하는 것:
  ✅ /sandbox 폴더 읽기/쓰기
  ✅ /tmp 폴더 읽기/쓰기
  ✅ 화이트리스트에 있는 외부 서버 접속

OpenShell이 금지하는 것:
  ❌ 시스템 파일 접근 (읽기조차 불가)
  ❌ 화이트리스트 외 네트워크 접속
  ❌ 허가받지 않은 프로세스 실행
```

**중요한 특징: 규칙은 시작 시 고정됨**

```
샌드박스 시작 → 보안 규칙 고정
                    ↓
              이후에는 변경 불가!
```

왜 이렇게 하나요? AI가 실행 중에 스스로 규칙을 바꾸는 것을 막기 위해서입니다.

---

### Layer 2: Privacy Router (프라이버시 라우터)

클라우드 AI(GPT-4, Claude 등)를 쓸 때는 내 데이터가 외부 서버로 전송됩니다.
Privacy Router는 이때 **개인식별정보(PII)를 자동으로 제거**합니다.

```
원래 메시지: "홍길동의 주민번호는 900101-1234567이고 전화번호는 010-1234-5678입니다"
                          |
                   [Privacy Router]
                          |
클라우드로 전송: "[이름]의 주민번호는 [PII_1]이고 전화번호는 [PII_2]입니다"
```

**어떤 정보를 제거하나?**
- 이름, 전화번호, 주소
- 주민등록번호, 여권번호
- 이메일 주소, 신용카드 번호
- 기타 식별 가능한 개인정보

**Privacy Router의 정책 기반 라우팅**

민감도에 따라 어떤 AI 모델을 사용할지 자동으로 결정합니다:

```
민감하지 않은 질문 → 클라우드 AI (GPT-4, Claude 등) 사용
개인정보 포함 → 로컬 AI (Nemotron) 사용 또는 PII 제거 후 전송
기밀 데이터 → 로컬 AI만 사용, 외부 전송 금지
```

---

### Layer 3: Intent Verification (인텐트 검증)

AI가 어떤 행동을 하기 전에 **"이 행동이 정말 의도된 것인가?"** 를 확인합니다.

예시:
```
사용자: "이 폴더 정리해줘"
AI: 파일 10개를 삭제하려고 함
  ↓
[Intent Verification]
  ↓
운영자에게 알림: "AI가 파일 10개를 삭제하려 합니다. 허용하시겠습니까?"
운영자: "예" 또는 "아니오" 선택
```

---

## 5. Nemoclaw의 네트워크 정책

OpenClaw는 기본적으로 허용(permit) 정책이었지만, Nemoclaw는 **기본 거부(default-deny) 정책**을 사용합니다.

### 기본 거부 정책이란?

```
OpenClaw 방식 (허용 기본):
  "명시적으로 금지하지 않은 것은 모두 허용"
  → 새로운 위협이 생겨도 막을 수 없음

Nemoclaw 방식 (거부 기본):
  "명시적으로 허용하지 않은 것은 모두 차단"
  → 화이트리스트에 없는 모든 접속 시도가 차단됨
```

### 네트워크 정책 예시

```yaml
# network-policy.yaml

network:
  default: deny  # 기본은 모두 차단

  allow:
    - host: "api.openai.com"
      port: 443
      methods: ["POST"]
      processes: ["openclaw-agent"]

    - host: "calendar.google.com"
      port: 443
      methods: ["GET", "POST"]
      processes: ["openclaw-agent"]
```

### 허용되지 않은 접속 시도 시

```
AI 에이전트가 허가받지 않은 서버에 접속 시도
                    |
           [OpenShell 인터셉트]
                    |
         운영자에게 TUI(터미널 UI) 알림:
         "AI가 example.com:443에 접속하려 합니다.
          허용하시겠습니까? [Y/N]"
```

---

## 6. Nemoclaw의 지원 플랫폼

NVIDIA는 자사 하드웨어를 염두에 두고 설계했습니다:

| 플랫폼 | 용도 |
|--------|------|
| NVIDIA GeForce RTX PC/노트북 | 개인 사용자 로컬 실행 |
| RTX PRO 워크스테이션 | 전문가/소규모 팀 |
| DGX Station | 기업용 온프레미스 |
| DGX Spark | 엣지/소형 서버 |

단, NVIDIA GPU가 없어도 CPU나 다른 GPU로도 실행은 가능합니다(성능 차이 있음).

---

## 7. Nemoclaw의 전체 구조 한눈에 보기

```
[사용자]
   |
   | (채팅 앱 또는 직접 입력)
   v
[OpenClaw 게이트웨이]  ←─── 포트 8080
   |
   v
[Intent Verification]  ← 행동 승인/거부
   |
   v
[Privacy Router]  ← PII 제거, 모델 라우팅 결정
   |
   ├─── 민감하지 않음 ──→ [클라우드 AI]
   │                       (GPT-4, Claude 등)
   │
   └─── 민감한 데이터 ──→ [로컬 AI]
                           (Nemotron 30B Nano)
   |
   v
[OpenShell 샌드박스]  ← 파일/네트워크 접근 제어
   |
   v
[실제 도구 실행]
(파일 접근, 이메일, API 호출 등)
```

---

## 8. Nemoclaw의 현재 한계

2026년 3월 기준 얼리 프리뷰 단계이므로:
- 프로덕션 환경 사용 권장 안 함
- 일부 기능이 불안정할 수 있음
- 문서가 아직 미완성인 부분 있음
- 빠르게 업데이트 중 (버전 변경 잦음)

---

다음 챕터: [04 - 로컬 실행 가이드](./04_local_setup.md)
