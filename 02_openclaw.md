# 챕터 02: OpenClaw 이해하기

> Nemoclaw의 기반이 된 OpenClaw가 무엇인지, 어떻게 동작하는지 이해합니다.

---

## 1. OpenClaw란?

**OpenClaw**는 내 컴퓨터에서 직접 실행되는 오픈소스 AI 개인 비서입니다.

쉽게 말하면: "내 컴퓨터에 설치해서 WhatsApp, 텔레그램, 슬랙 같은 채팅 앱을 통해 AI와 대화하고, AI가 내 대신 이메일 보내기, 캘린더 관리, 파일 정리 같은 일을 자동으로 해주는 시스템"입니다.

### 핵심 특징

| 특징 | 설명 |
|------|------|
| 로컬 우선 | 대화 내용, 기억, 기술들이 모두 내 컴퓨터에 저장됨 |
| 클라우드 없음 | 외부 SaaS 서비스에 데이터를 보내지 않음 |
| 다양한 채팅 앱 연동 | WhatsApp, 텔레그램, Slack, Discord, iMessage 등 지원 |
| 항상 켜진 상태 | 이메일 확인, 캘린더 관리 등을 자동으로 처리 |

---

## 2. OpenClaw의 구조

```
[채팅 앱 (WhatsApp 등)]
        |
        v
[OpenClaw 게이트웨이]  ← 모든 요청의 입구
        |
   ┌────┴────┐
   v         v
[AI 모델]  [스킬/도구]
(GPT-4 등) (이메일, 파일, 달력...)
        |
        v
[메모리 시스템]
(마크다운/YAML 파일로 저장)
```

### 주요 컴포넌트 설명

**게이트웨이(Gateway)**
- OpenClaw의 중심부입니다
- 채팅 앱에서 오는 메시지를 받아 AI에게 전달합니다
- AI의 응답을 채팅 앱으로 다시 보냅니다
- 기본 포트: `8080`

**스킬(Skills)**
- AI가 실제로 무언가를 "할 수 있는" 능력입니다
- 예: 이메일 보내기, 구글 캘린더 접근, 파일 읽기/쓰기
- YAML 파일로 정의됩니다

**메모리 시스템**
- AI가 이전 대화를 기억하는 방식입니다
- 마크다운(.md)과 YAML(.yaml) 파일로 저장됩니다
- 내 컴퓨터 로컬에만 보관됩니다

---

## 3. OpenClaw의 동작 흐름 예시

"오늘 오후 3시에 팀 미팅 잡아줘"라고 WhatsApp에 메시지를 보내면:

```
1. WhatsApp → [OpenClaw 게이트웨이] 메시지 도착
2. 게이트웨이 → [AI 모델] "일정 추가 요청 분석"
3. AI 모델 → [캘린더 스킬] "오후 3시 팀 미팅 일정 생성"
4. 캘린더 스킬 → Google Calendar API 호출
5. 성공 → 게이트웨이 → WhatsApp "오후 3시 팀 미팅 추가했습니다"
```

---

## 4. OpenClaw의 보안 설정

OpenClaw는 많은 설정을 할 수 있는데, 잘못 설정하면 보안 문제가 생깁니다.

### 중요한 보안 설정들

```yaml
# config.yaml 예시

gateway:
  bind: "127.0.0.1:8080"  # 올바른 설정: 로컬에서만 접근
  # bind: "0.0.0.0:8080"  # 위험한 설정: 외부에서도 접근 가능

  auth:
    mode: "token"  # 인증 방식 (token, password, none)
    token: "my-secret-token-here"
```

### OpenClaw의 보안 감사 명령어

OpenClaw는 설정 문제를 자동으로 검사하는 기능이 있습니다:

```bash
openclaw security audit
```

이 명령어는 다음을 확인합니다:
- 게이트웨이 인증이 설정되어 있는가?
- 위험한 권한이 부여되어 있는가?
- 파일 시스템 권한이 적절한가?
- 외부에 노출된 서비스가 있는가?

---

## 5. OpenClaw의 알려진 보안 문제

OpenClaw는 강력한 도구이지만, 기본적으로 **허용(permissive) 정책**으로 설계되어 있습니다.
즉, "명시적으로 금지하지 않으면 허용"하는 방식입니다.

### 실제로 발생한 문제들

```
발견된 문제: 40,000개 이상의 OpenClaw 인스턴스가
            게이트웨이를 공개 IP(0.0.0.0)에 바인딩한 채로
            인터넷에 노출되어 있었음

결과: 누구나 이 게이트웨이에 접근해서
     AI 에이전트를 원격 제어할 수 있는 상태
```

이런 문제가 생기는 이유:
1. 기본 설정이 너무 관대함
2. 사용자가 보안 설정을 별도로 하지 않으면 취약한 상태
3. "일단 동작하게 하자"는 설정이 기본값으로 되어 있음

---

## 6. OpenClaw 실행 방법 (개요)

```bash
# 1. 설치
pip install openclaw

# 2. 초기화
openclaw init

# 3. 실행
openclaw start

# 4. 게이트웨이 접속
# http://localhost:8080 에서 관리 UI 확인
```

OpenClaw는 Python으로 만들어져 있고, 설정 파일은 `~/.openclaw/config.yaml`에 있습니다.

---

## 7. OpenClaw가 중요한 이유

Nemoclaw는 OpenClaw를 **기반**으로 하되, **보안을 대폭 강화**한 프로젝트입니다.

즉, OpenClaw를 이해하면 Nemoclaw가 "무엇을 문제로 보고 어떻게 개선했는지"를 이해할 수 있습니다.

```
OpenClaw = 강력하지만 보안이 상대적으로 느슨한 AI 에이전트
Nemoclaw = OpenClaw + 기업 수준의 보안 레이어
```

---

다음 챕터: [03 - Nemoclaw 이해하기](./03_nemoclaw.md)
